#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    ll t;
    cin>>t;
    while (t--)
    {
        ll n, q;
        cin>>n>>q;
        vector <ll> arr(n + 1, 0);
        for(ll i = 1; i <= n; i++)
        {
            ll a;
            cin>>a;
            arr[i] = a;
        }
        while(q--)
        {
            ll l, r;
            cin>>l>>r;

            
        }
    }
    
    return 0;
}